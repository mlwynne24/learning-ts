# Project 3: Real-Time Message Processor

*Unlocks at Week 7*

Build a message queue system with per-group concurrency control, polling-based
ingestion, and async task execution. Directly mirrors nanoclaw's group-queue pattern.

**Skills practised:** async patterns, concurrency control, queues, WebSockets/polling, structured logging

**TS strength over Python:** Node's event loop handles concurrent I/O without threading complexity
