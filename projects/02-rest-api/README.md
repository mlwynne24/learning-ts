# Project 2: REST API

*Unlocks at Week 6*

Build a typed REST API server — your TypeScript equivalent of FastAPI. Handles
routing, middleware, request/response types, and error handling.

**Skills practised:** Express/Fastify, typed routes, middleware, Zod request validation

**Python equivalent:** FastAPI with Pydantic models
