# Project 1: CLI Tool

*Unlocks at Week 4*

Build a command-line utility that reads structured data (CSV/JSON), validates it
with runtime type checking, and outputs a formatted report.

**Skills practised:** modules, file I/O, argument parsing, Zod validation, error handling

**Python equivalent:** an argparse-based script with pydantic validation
